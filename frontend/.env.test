# TEST ENVIRONMENT - PostgreSQL Configuration
# Used by test battery (db-schema.test.ts)

# PostgreSQL Test Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dentiagest
DB_USER=postgres
DB_PASSWORD=11111111

# GraphQL Test Endpoint
VITE_GRAPHQL_URI=http://localhost:8005/graphql

# Note: Password is temporary (TODO: Change to secure password later)
