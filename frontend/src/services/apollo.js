/**
 * 🚀 APOLLO WEBPACK COMPATIBLE VERSION
 * Simplificado para que Webpack deje de llorar
 */

// Import the actual Apollo implementation
import apollo from './api/index.ts';

// Re-export it
export default apollo;
